void main(){

  // dart is optinally types language it can behave like statucally type or dynamically typed 

  //static type example

  var name = "Priyanshu";
   //name = 40;//it through the error just because it behave like the statically type lanuage

  print(name);

   dynamic nameOne = "Priyanshu";
   nameOne = "1705";//it not through the error just because it behave like the dynamically type lanuage
  
  print(name);
}